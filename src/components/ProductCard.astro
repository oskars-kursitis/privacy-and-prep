---
const { title, image, excerpt, slug } = Astro.props;
---
<div class="border rounded-xl p-4 shadow-sm">
  <img src={image} alt={title} class="w-full h-40 object-cover rounded-lg mb-3" loading="lazy" />
  <div class="font-semibold mb-1">{title}</div>
  <p class="text-sm text-gray-600 dark:text-gray-400 mb-3">{excerpt}</p>
  <a class="inline-block px-3 py-2 rounded bg-gray-900 text-white dark:bg-white dark:text-gray-900 no-underline" href={`/go/${slug}/`}>Check price</a>
</div>
